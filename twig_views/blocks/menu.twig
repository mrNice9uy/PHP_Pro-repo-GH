<ul class="nav justify-content-end">
    {% for item in menu %}
        <li class="nav-item">
            <a class="nav-link active" href="{{ item.link }}">
                {{ item.title }}
            </a>
        </li>
    {% endfor %}
</ul>