<!doctype html>
<html lang="en">
{{ include('blocks/head.twig') }}
<body>
<div class="row justify-content-center">
    <div class="col-md-6">
        {{ include('blocks/menu.twig') }}
        <main>
            {{ content|raw }}
        </main>
        <footer>
        </footer>
    </div>
</div>
</body>
</html>